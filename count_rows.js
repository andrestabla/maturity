import { getSql } from './lib/db.js'; const sql = getSql(); const rows = await sql`SELECT 'courses' as name, COUNT(*)::text FROM maturity_courses UNION ALL SELECT 'tasks', COUNT(*)::text FROM maturity_tasks UNION ALL SELECT 'library_resources', COUNT(*)::text FROM maturity_library_resources UNION ALL SELECT 'alerts', COUNT(*)::text FROM maturity_alerts;`; console.log(JSON.stringify(rows)); process.exit(0);
